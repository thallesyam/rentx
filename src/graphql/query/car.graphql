query Car($slug: String!) {
  car(where: { slug: $slug }) {
    id
    name
    company
    capacity
    description
    kmH
    kmS
    price
    slug
    supply
    manual
    images {
      url
    }
  }
}
