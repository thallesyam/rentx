mutation CreateClient(
  $name: String!
  $email: String!
  $cnh: Int!
  $password: String!
) {
  createClient(
    data: { name: $name, email: $email, cnh: $cnh, password: $password }
  ) {
    id
  }
}
